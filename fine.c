#include<stdio.h>
int main(){
    int latedays;
    printf("enter the late days: ");
    scanf("%d",&latedays);
    int charges;
    int fine;
    fine=latedays*charges;

    if(latedays=0){
        printf("no fine");
    }
else if(latedays>0 && latedays<=5)
    

}